#include<stdio.h>
int main(){
    char Str[100];

    printf("Enter the string to find the Pangarm: ");
    fgets(Str, sizeof(Str), stdin);

    for(i=0; i<sizeof(Str); i++){
        if(Str[i]=='A' )
    }
}